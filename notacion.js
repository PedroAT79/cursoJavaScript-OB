//Notacion:
//; --> el punto y coma: delimita el final de una linea.
const a = 4;
//no es obligatorio usarlo pero lo mejor es usarlo siempre al final de cada linea.
// . --> El punto se usa en los objetos para acceder a los atributos (propiedades del objeto).
//[] --> Corchetes: Square Brackets -- se usan para listas, arrays, para definirlos o acceder a algun valor
const array = [1,2,3];
console.log(array[2]);
//() --> funciones
function suma(a,b);
//{} --> para objetos, funciones y estructuras de control.
const movil = {
    anchura:5,
    altura:10
}

if(true){
    //todo lo que haya dentro de las llaves.
}


